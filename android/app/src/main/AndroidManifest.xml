<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.summation.android.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinCall">
        <activity android:name="app.summation.android.demo.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="app.summation.android.demo.GatewayQueryResultActivity" />
        <activity android:name="app.summation.android.demo.GatewayReadResultActivity" />
        <activity android:name="app.summation.android.demo.GatewayGetResultActivity" />
    </application>

</manifest>